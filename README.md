# Social Cues App

Production deployment package for Social Cues.

This repository contains the Social Cues app shell, server routes, provider OAuth readiness endpoints, Meta/X integration work, Supabase schema, and Vercel deployment configuration.

Secrets are intentionally excluded. Configure runtime values in Vercel environment variables.
